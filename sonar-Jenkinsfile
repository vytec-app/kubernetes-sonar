pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                
              git branch: 'dev', url: 'https://github.com/vytec-app/kubernetes-sonar.git'

               
                sh "/usr/bin/kubectl apply -f sonar-mysql-deployment.yaml -f sonar-svc-mysql.yaml -f sonar-deployment.yaml -f sonar-svc.yaml"

               
            }

            
        }
    }
}
